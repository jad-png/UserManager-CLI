package main

import (
	"fmt"
)

func main() {
	var power int = 9000
	fmt.Printf("Power: %d\n", power)

	name := "yasser"
	fmt.Printf("new Power: %s\n", name)

	last_name, num := "elouariachi", 11
	fmt.Printf("player name: %s, player last name: %s, player shirt number: %d\n", name, last_name, num)
}
